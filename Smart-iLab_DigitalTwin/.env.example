# Vite env — only variables prefixed with VITE_ are exposed to the browser bundle.
# Copy to .env or .env.local and edit. Vite reads from the project root at build time.

VITE_API_URL=http://localhost
