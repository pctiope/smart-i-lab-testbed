# Root Documentation Index

This folder contains the active root-level documentation for the DuckDB Bronze/Silver/Gold pipeline and the migrated Zone 5 runtime.

## Documents

- `AI231_zone5.md` — Zone 5-only backup scope, device IDs, and targeted bootstrap/live/training commands

## Suggested Read Order

1. Start with `README_db.md` for the end-to-end pipeline flow.
2. Use `RUNTIME_GUIDE.md` for local bring-up, live polling, and smoke validation.
3. Use `README_training_migrated.md` for the Zone 5 migrated training-input path and validation details.

## Related Folder

The root test suite now lives under `../TEST/`.
